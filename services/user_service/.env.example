#User Service Environment
# Example configuration for the FastAPI User Service.

# Database Configuration

# PostgreSQL connection string (use asyncpg for async support)
# Format: postgresql+asyncpg://<USER>:<PASSWORD>@<HOST>:<PORT>/<DATABASE>
DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/user_service_db


# Redis Configuration

# Redis connection string for caching, idempotency, and rate-limiting
REDIS_URL=redis://redis:6379/0

# Server Configuration
# Port on which the FastAPI app will run inside the container
PORT=3001

# Security Configuration

# Secret key for signing tokens, session management, etc.
# Generate a secure one with: `openssl rand -hex 32`
SECRET_KEY=your_super_secret_key_here

# Idempotency Configuration
# TTL (in seconds) for idempotency keys stored in Redis
IDEMPOTENCY_TTL=3600
